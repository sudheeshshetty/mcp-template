{
  "name": "workerhub-mcp-demo",
  "private": true,
  "version": "0.0.0",
  "description": "Isolated MCP + Ollama chat demo for WorkerHub (delete mcp/ to remove)",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @workerhub-mcp/app build && concurrently -n mcp,web -c magenta,green \"pnpm --filter @workerhub-mcp/app dev\" \"pnpm --filter @workerhub-mcp/web dev\"",
    "dev:mcp": "pnpm --filter @workerhub-mcp/app dev",
    "dev:web": "pnpm --filter @workerhub-mcp/web dev:ui",
    "dev:api": "pnpm --filter @workerhub-mcp/web dev:api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
