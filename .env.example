# MCP server (mcp-server/)
MCP_PORT=8788
MCP_SERVER_URL=http://127.0.0.1:8788/mcp

# Optional: backend for sample list_employees tool (sample-server/)
SAMPLE_API_URL=http://127.0.0.1:9000

# Chat API host (chat-api/) — widget calls this URL only
CHAT_API_PORT=8787
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.1
# Comma-separated origins, or * for local demo
CORS_ORIGINS=http://localhost:5174,http://127.0.0.1:5174,http://localhost:8787

# Optional sample API port
SAMPLE_PORT=9000
